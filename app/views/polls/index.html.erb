<h1>Pollster</h1>
<%= button_to 'New Poll', new_poll_path, :method => :get,  :class => 'button new_poll' %>
<ul class="accordion">
  <li class="active">
    <% @polls.each do |poll| %>
      <div class="title">
        <h5><%= poll.title %></h5>
      </div>
      <div class="content">
        <p><%= poll.author %></p>
        <p>(responses, total responses, button to respond)</p>
      </div>
    <% end %>
  </li>
</ul>

















